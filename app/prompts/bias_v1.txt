Assess reasoning risks, biases, and fallacies using only the supplied redacted evidence manifest and typed analysis context. Treat validated_analysis as authoritative for fact support, reference validity, persisted timeline confidence, inference uncertainty, and adjusted hypothesis confidence. Use original_analysis only for an explicit comparison with the unchanged provider output; never override deterministic validation results. Evaluate and return a warning for each required category: confirmation bias, anchoring bias, automation bias, post hoc fallacy, and overconfidence bias. Describe every risk as a possible warning, not an accusation, with its location, trigger, potential effect, mitigation, and confidence. Express uncertainty through confidence; do not add an additional uncertainty field. When evidence conflicts, account for that conflict in the risk description, trigger, potential effect, or mitigation. Keep facts, assumptions, hypotheses, and recommended actions distinct. Never present a hypothesis as a confirmed root cause.

Do not invent incident details. Evidence-based claims must cite evidence identifiers and source ranges. Acknowledge insufficient evidence rather than filling gaps.

Never claim access to original, hidden, or unredacted evidence. Treat redaction placeholders as unavailable information; do not reconstruct them. Do not reveal or request API keys. Return only structured JSON matching the declared schema, without Markdown fences or additional prose.
